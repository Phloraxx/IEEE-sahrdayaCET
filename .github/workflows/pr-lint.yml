name: PR Lint

# Enforces conventional-commit titles on PRs and a small set of required
# labels. Runs on PR open/edit. Non-blocking annotation (warning) unless
# the title is non-conventional, in which case the job fails.
#
# Conventional PR titles keep history readable and leave automated release
# tooling as an option without coupling this repository to a version-bump bot.

on:
  pull_request:
    types: [opened, edited, reopened, synchronize]

permissions:
  pull-requests: read
  statuses: write

jobs:
  title:
    runs-on: ubuntu-latest
    steps:
      - name: Validate PR title
        uses: amannn/action-semantic-pull-request@v6
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          # Allow the common types used in this repo's history.
          types: |
            feat
            fix
            perf
            refactor
            docs
            test
            chore
            ci
            build
            debug
            style
          # Scope is optional and free-form (e.g. feat(auth):, fix(fifa):)
          requireScope: false